export const waveSurferOptions = {
    container: '',
    waveColor: '#0a4ab8',
    progressColor: '#55627c',
    // backend: 'MediaElement',
    backgroundColor: 'transparent',
    forceDecode: true,
    loaderColor: 'red',
    responsive: true,
    skipLength: 2,
    cursorColor: '#2fd5ff',
    height: 0,
    barWidth: 2,
    barRadius: 2,
    normalize: true,
    hideScrollbar: true,
    barHeight: 1, // the height of the wave
    barGap: null,
    barMinHeight: 6,
    // xhr:{ cache: 'default', mode: 'cors', method: 'GET', credentials: 'same-origin', redirect: 'follow', referrer: 'client'},
    plugins: null
}

export const timeLineOptions = {
    container: '',
    height: 24,
    unlabeledNotchColor: '#2FD5FFFF',
    primaryColor: '#2FD5FFFF',
    secondaryColor: 'rgba(47,213,255,0.5)',
    primaryFontColor: '#2FD5FFFF',
    secondaryFontColor: 'rgba(47,213,255,0.5)',
    fontSize: 14,
    primaryLabelInterval: 30
}
